zerops:
  - setup: app
    build:
      base: nodejs@20
      buildCommands:
        - pnpm i
        - pnpm run build
      deployFiles:
        - dist/recipe-angular-static/browser/~
    run:
      base: static
      routing:
        redirects:
          - from: /*
            to: /index.html

